import 'package:flutter/material.dart';

class OnBoardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ascon OnBoard'),
      ),
      body: Center(
        child: Text('Welcome to the Ascon OnBoard!'),
      ),
    );
  }
}
